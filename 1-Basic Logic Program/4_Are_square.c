// . Find Area of Square formula : a = a2

#include <stdio.h>
main()
{
	int a,Area;
	printf("Enter the Length of the square:");
	scanf("%d", &a);
	Area= a * a;
	printf("\n Area of the square is %d", Area);
}
